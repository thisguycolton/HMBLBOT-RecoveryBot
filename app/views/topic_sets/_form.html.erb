<%= form_with(model: topic_set) do |form| %>
  <% if topic_set.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(topic_set.errors.count, "error") %> prohibited this topic_set from being saved:</h2>

      <ul>
        <% topic_set.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
